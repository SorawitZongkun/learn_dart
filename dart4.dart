// Dynamic Variable

void main() {
  dynamic x = 10.11;
  print("1. X = $x");
  x = "สุดหล่อ";
  print("2. X = $x");
  x = 100;
  print("3. X = $x");
}

// กรณีที่ใช้ เช่น ไปดึงข้อมูลจาก server แล้วไม่รู้ว่าข้อมูลที่ส่งมาเป็นชนิดที่ตายตัวหรือไม่
